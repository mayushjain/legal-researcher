version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.fullstack
    ports:
      - "3000:3000"
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./outputs:/usr/src/app/outputs
    restart: unless-stopped
